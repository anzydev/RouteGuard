{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "preinstall": "node ./scripts/bootstrap-pnpm.cjs",
    "postinstall": "node ./scripts/postinstall-workspace.cjs",
    "dev": "PORT=5173 BASE_PATH=/ pnpm --filter @workspace/transit run dev",
    "build": "pnpm run typecheck:libs && pnpm --filter @workspace/transit run build",
    "start": "bash ./start.sh",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck"
  },
  "private": true,
  "devDependencies": {
    "concurrently": "^9.1.0",
    "typescript": "~5.9.2",
    "prettier": "^3.8.1"
  }
}
